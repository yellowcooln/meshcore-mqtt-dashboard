services:
  mqtt-dashboard:
    build: ./backend
    container_name: mqtt-dashboard
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-8081}:8081"
    restart: unless-stopped
    volumes:
      - ./data:/data
